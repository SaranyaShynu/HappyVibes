<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Happy Vibes ✨</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: "Poppins", sans-serif;
      background: linear-gradient(135deg, #8bc6ec, #9599e2);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    #myLandbot {
      width: 350px;
      height: 500px;
      border-radius: 22px;
      overflow: hidden;
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
      animation: fadeIn 0.7s ease-in-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>

  <!-- Landbot Chat Container -->
  <div id="myLandbot"></div>

  <script type="module" src="https://cdn.landbot.io/landbot-3/landbot-3.0.0.mjs"></script>
  <script type="module">
    var myLandbot = new Landbot.Container({
      container: '#myLandbot',
      // ✅ Replace this URL with your actual published Landbot config URL
      configUrl: '<%= landbotUrl %>'
    });
  </script>

</body>
</html>
